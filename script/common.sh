set -e # Exit immediately if a command exits with a non-zero status.
set -o pipefail # Exit status of pipeline is right-most non-zero exit status
set -u # Exit immediately if a variable is not defined.
